<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iheima.web_tlisas_management.mapper.EmpMapper">
    <select id="list" resultType="com.iheima.web_tlisas_management.pojo.Emp">
        SELECT e.*, d.name as deptName FROM emp e left join dept d on e.dept_id = d.id 
        <where> 
            <if test="name != null and name != ''">
            e.name LIKE concat('%', #{name}, '%') 
            </if>
            <if test="gender != null">
                AND e.gender = #{gender} 
            </if>
            <if test="begin != null and end != null">
                AND e.entry_date BETWEEN #{begin} AND #{end} 
            </if>
        </where>
        ORDER BY e.update_time DESC
    </select>

    <insert id="insert" parameterType="com.iheima.web_tlisas_management.pojo.Emp">
        INSERT INTO emp (username, name, gender, phone, job, salary, image, entry_date, dept_id, create_time, update_time) 
        VALUES (#{username}, #{name}, #{gender}, #{phone}, #{job}, #{salary}, #{image}, #{entryDate}, #{deptId}, #{createTime}, #{updateTime})
    </insert>
</mapper>